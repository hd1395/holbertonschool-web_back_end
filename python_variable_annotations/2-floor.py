#!/usr/bin/env python3
import math

"""
Basic annotations - floor
"""


def floor(n: float) -> int:
    """
    This function takes a float n and returns the
    floor of the float as an integer.
    """
    return int(math.floor(n))
